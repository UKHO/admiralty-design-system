import { SortHeaderDirective } from './sort-header.directive';

describe('SortHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SortHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
