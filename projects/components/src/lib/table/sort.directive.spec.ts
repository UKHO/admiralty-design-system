import { UkhoSort } from './sort.directive';

describe('SortDirective', () => {
  it('should create an instance', () => {
    const directive = new UkhoSort();
    expect(directive).toBeTruthy();
  });
});
