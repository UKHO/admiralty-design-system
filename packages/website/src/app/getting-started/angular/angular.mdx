import {AdmiraltyTabGroup, AdmiraltyTab} from '@ukho/admiralty-react';

# Angular

## Step 1 - Install Dependencies

The first step is to install the package so that it can be used in your Angular project. You will need

<AdmiraltyTabGroup>
  <AdmiraltyTab label="NPM">
    ```bash
    npm install @ukho/admiralty-angular
    ```
  </AdmiraltyTab>
  <AdmiraltyTab label="Yarn">
    ```bash
    yarn add @ukho/admiralty-angular
    ```
  </AdmiraltyTab>
</AdmiraltyTabGroup>
