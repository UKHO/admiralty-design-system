
| Name | Description |
| --- | --- |
| `admiraltySortChange` | Emitted when the user clicks the sort button. The `detail` contains the<br /><br />new `direction` value (`'ascending'`, `'descending'`, or `'none'`). |

