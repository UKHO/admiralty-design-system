# @ukho-internal/admiralty-docs

This package stores the auto generated markdown docs files that are used to generate the website content.

**Anything that you change here will get overwritten on build**
